\chapter[Appendix \ref{appendix_T_S_Field_Formulation}]{Total/Scattered Field Formulation}{Total/Scattered Field Formulation}\label{appendix_T_S_Field_Formulation}\index{FDTD!total/scattered field}
\begin{spacing}{1.25}
\vskip 20pt
\noindent
\rule{0.49\textwidth}{0.75pt} $_{\Diamond}$ \rule{0.49\textwidth}{0.75pt}\\
{\Large{\textbf{Preface}}}\\
Problems like analysis of an antenna in receiving mode, calculation of radar cross section etc. deal with plane wave. A brief review of the method to formulate plane wave with in FDTD lattice is given here. For more details readers may refer \cite{Sullivan_book_00}. 
\\
\rule{0.49\textwidth}{0.75pt} $_{\Diamond}$ \rule{0.49\textwidth}{0.75pt}
\end{spacing}
\newpage







\begin{figure}
 \centering
 \subfigure[]{\label{Fig:total_scattered_field_3d}
      \includegraphics[scale=0.4475]{total_scattered_field_3d}
    }\goodgap
 \subfigure[]{\label{Fig:total_scattered_field_2d}
      \includegraphics[scale=0.5]{total_scattered_field_2d}
    }
 \caption[Total field/scattered field of FDTD problem space]{Total field/scattered field (a) Three-dimensional. (b) Two-dimensional.}
\label{Fig:total_scattered_field}
\end{figure}
To simulate a plane wave in a FDTD program, the problem space is be divided up into total field and scattered field regions as shown in the Fig.~\ref{Fig:total_scattered_field}. The method given in \cite{Sullivan_book_00} is detailed here.

Fig.~\ref{Fig:total_scattered_field} illustrates how this is accomplished. First an auxiliary one-dimensional buffer called the incident array is considered. As this is a one-dimensional array, it is easy to generate a plane wave: a source point is chosen and the incident Ez field is just added
at that point. Then a plane wave propagates away in both directions. Since it is a one-dimensional
array, the boundary conditions are perfect \cite{Sullivan_book_00}.

%The problem, like prediction of the performance of an antenna in receiving mode working as an EM field sensor, deal with the plane waves. In order to simulate a plane wave in a FDTD program, the problem space will be divided up into two regions, namely the total field and scattered field regions as shown in the Fig.~\ref{Fig:total_scattered_field}. The reasons behind this division are that the propagating plane wave should not interact with the absorbing boundary conditions and the load on the absorbing boundary conditions should be minimized \cite{Sullivan_book_00}. These boundary conditions are not perfect, i.e., a certain portion of the impinging wave reflects back into the problem space. By subtracting the incident field, the amount of the radiating field hitting the boundary is minimized, thereby reducing the amount of error.

%Fig.~\ref{Fig:total_scattered_field} illustrates how this is accomplished. First an auxiliary one-dimensional buffer called the incident array is considered. As this is a one-dimensional array, it is easy to generate a plane wave: a source point is chosen and the incident $E_z$ field is just added at that point. Then a plane wave propagates away in both directions. Since it is a one-dimensional array, the boundary conditions are perfect \cite{Sullivan_book_00}.

\begin{figure}
  \begin{center}
    \leavevmode
      \includegraphics[scale=0.65]{point_is_in_either_the_tf_or_the_sf}
    \caption{Each point is in either total field or in the scattered field.}
    \label{Fig:point_is_in_either_the_tf_or_the_sf}
  \end{center}
\end{figure}

As shown in Fig.~\ref{Fig:point_is_in_either_the_tf_or_the_sf} the two-dimensional field at every point in the problem space is either in the total field or it is not; no point lies on the border. Therefore, if a point is in the total field but it uses points outside to calculate the spatial derivatives when updating its value, it must be modified. The same is true in case a point lying just outside that uses points inside the total field. A plane wave generated in one plane of the three-dimensional problem space, in this case an $XZ$ plane, at $j=ja$ and subtracted out at $j=jb$. Therefore, in free space with no obstacle in the total field, we should see only $E_z$ and $H_x$. The plane wave is generated at one side and subtracted out at other side by adding to $E$ or $H$ fields that are on the boundary and subtracting from $E$ or $H$ fields that are next to the boundary. For the three dimensional case, there is additional surface at $k=ka$ and $k=kb$. The Fig.~\ref{Fig:total_scattered_field_3d} illustrates the $k=ka$ \& $k=kb$ boundaries. For this reason it is needed to make these modifications for the incident array. The modifications are:
\begin{enumerate}
\item The $E_y$ value at $k=ka$ \& $k=kb$:
\begin{subequations}\label{tf_sf_modified_3d_1}
\begin{align}
E_y\left(i,j+1/2,ka\right)&=E_y\left(i,j+1/2,ka\right)-0.5\cdot H_{x\_inc}\left(j\right)\\
E_y\left(i,j+1/2,kb+1\right)&=E_y\left(i,j+1/2,kb+1\right)+0.5\cdot H_{x\_inc}\left(j\right)
\end{align}
\end{subequations}

\item The $E_z$ value at $j=ja$ \& $j=jb$:
\begin{subequations}\label{tf_sf_modified_1}
\begin{align}
E_z\left(i,j_a,k+1/2\right)&=E_z\left(i,j_a,k+1/2\right)+0.5\cdot H_{x\_inc}\left(j_a-1/2\right)\\
E_z\left(i,j_b,k+1/2\right)&=E_z\left(i,j_b,k+1/2\right)-0.5\cdot H_{x\_inc}\left(j_b+1/2\right)
\end{align}
\end{subequations}

\item The $H_x$ field just outside $j=ja$ \& $j=jb:$
\begin{subequations}\label{tf_sf_modified_2}
\begin{align}
H_x\left(i,j_a-1/2,k\right)&=H_x\left(i,j_a-1/2,k\right)+0.5\cdot E_{z\_inc}\left(j_a\right)\\
H_x\left(i,j_b+1/2,k\right)&=H_x\left(i,j_b+1/2,k\right)-0.5\cdot E_{z\_inc}\left(j_b\right)
\end{align}
\end{subequations}

\item The $H_y$ field just outside $i=ia$ \& $i=ib:$
\begin{subequations}\label{tf_sf_modified_3}
\begin{align}
H_y\left(i_a-1/2,j,k\right)&=H_x\left(i_a-1/2,j,k\right)-0.5\cdot E_{z\_inc}\left(j_a\right)\\
H_y\left(i_b+1/2,j,k\right)&=H_x\left(i_b+1/2,j,k\right)+0.5\cdot E_{z\_inc}\left(j_b\right)
\end{align}
\end{subequations}
\end{enumerate}





















